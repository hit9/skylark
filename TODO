add parameter:autocommit to Database
add like,bettween
return type of each query

all use one query

##########################################################

change parser for runtime method name to private format

concat for field objects
