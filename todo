transaction:decorator

add Post.name = User.name+"str"+Post.name support. + operator for Field,+ operator for expr

add like, between for where

nose tests

delete return cast to int, select_result.count cast to int
